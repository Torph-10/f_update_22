[project]
name = "fly-in"
version = "0.1.0"
description = "Multi-drone time-space pathfinding and turn-based simulation engine"
requires-python = ">=3.10"
dependencies = [
    "webcolors>=24.0.0",
]

[dependency-groups]
dev = [
    "flake8>=7.0.0",
    "mypy>=1.10.0",
]